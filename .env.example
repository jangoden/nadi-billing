# Set the public origin before deployment to enable canonical URLs and sitemap.
NEXT_PUBLIC_SITE_URL=
# Optional real application demo URL. Without it, /demo shows the local flow simulator.
NADI_DEMO_URL=
